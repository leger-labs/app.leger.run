the idea of adding a dedicated prompts builder section is to make it so that i no longer have to writeevery time:


your role as Orchestrator will be to write high quality specialized handoff system prompts to give to claude code to implement each change with surgical precision. your handoff prompts will not contain any code, as the claude code agent has full access to the underlying codebase for the webapp.


-
makes me think of the raycast macos extension with hwo that behaves. 
